# CI for deepseek-harness-app — runs the gate aggregate as two parallel
# jobs so a doc-only change fails in seconds instead of behind the Flutter
# suite. Both are required statuses on `master`
# (`CI / docs (pull_request)`, `CI / code (pull_request)`).
#
#   docs  python3 scripts/verify_all.py docs — python only; needs the
#         reference submodule (launcher-icon drift gate reads it)
#   code  python3 scripts/verify_all.py code — flutter analyze, the full
#         test suite, the import gate
#
# The reference submodule is fetched from the official
# deepseek-harness repository (see .gitmodules); a public clone can
# initialize it without any internal access.
name: CI

on:
  push:
    branches: ["master"]
  pull_request:
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with reference submodule)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Doc gates (links, budgets, notes, skills, launcher icons)
        run: python3 scripts/verify_all.py docs

  code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with reference submodule)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Flutter 3.47.1 stable
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.47.1
          channel: stable
          cache: true

      # Warm the pub cache outside any gate timeout: flutter's bootstrap
      # `pub get --example` talks to pub.dev for version checks, and a cold
      # or flaky-proxied run can stall for minutes — which ate the whole
      # 600s flutter-analyze gate in the past. Resolving here first makes
      # the gate's internal pub gets fast cache hits.
      - name: Warm pub cache
        run: cd flutter && flutter pub get

      - name: Code gates (analyze, tests, imports)
        run: python3 scripts/verify_all.py code