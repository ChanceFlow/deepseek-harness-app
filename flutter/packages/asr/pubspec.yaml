name: asr
description: ASR model catalog, download state machine, Hugging Face / hf-mirror source clients, and model registry.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  test: ^1.24.0
