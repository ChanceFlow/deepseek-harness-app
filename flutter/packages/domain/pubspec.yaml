name: domain
description: Neutral UI-facing models and the ChatRepository contract.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dev_dependencies:
  test: ^1.24.0
