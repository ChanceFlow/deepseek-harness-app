name: harness_adapter
description: The only package that understands the dsh wire protocol.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  domain: ^0.1.0
  network: ^0.1.0

dev_dependencies:
  fake_async: ^1.3.1
  test: ^1.24.0
