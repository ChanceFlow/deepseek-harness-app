name: app
description: Flutter UI for the DeepSeek Harness client.
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  domain: ^0.1.0
  network: ^0.1.0
  harness_adapter: ^0.1.0
  flutter_riverpod: ^3.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
